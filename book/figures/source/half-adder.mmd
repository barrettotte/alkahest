%% Evaluated Mermaid source; publication uses a deterministic accessible SVG.
flowchart LR
  A[Input A] --> XOR[XOR]
  B[Input B] --> XOR
  A --> AND[AND]
  B --> AND
  XOR --> S[Sum]
  AND --> C[Carry]
